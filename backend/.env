# Server
PORT=5000
NODE_ENV=development

# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=collservice

# SMTP - set these to enable real email sending. Leave empty for development to use debugCode.
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=6aef128d08c16b
SMTP_PASS=210e1aa2d084bc
SMTP_FROM="Coll Service <no-reply@mailtrap.io>"

# Optional: set frontend origin used by CORS if needed
# FRONTEND_ORIGIN=http://localhost:5173
